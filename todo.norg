* TODO
 ** Lambda
    - do not use names as lambda binders but use a name plus an internal number that inctement automaticaly as lambda are nested

 ** Binders
    - the `data Name e` must not be exported and only created by `RecE`
